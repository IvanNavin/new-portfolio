// `ogl` ships no type declarations and there is no @types/ogl. The Orb
// background (from React Bits) only needs the module to resolve; typing it as
// `any` is acceptable for a self-contained WebGL helper.
declare module "ogl";
