/**
 * Source-of-truth for the Contact page links + the Cal.com booking handle.
 * Co-located so the slug, email and socials live in one place.
 */

/** Cal.com booking handle (`<username>` shows the event-type picker). */
export const CAL_BOOKING_LINK = "ivan-holovko";

/** Cal.com instance origin — the account lives on the EU tier. */
export const CAL_ORIGIN = "https://cal.eu";

/** Cal.com embed loader for the EU instance (cal.com URL 404s for EU accounts). */
export const CAL_EMBED_SCRIPT_SRC = "https://app.cal.eu/embed/embed.js";

/** Direct contact addresses, surfaced alongside the booking CTA. */
export const CONTACT_EMAIL = "ivan.holovko@example.com";
export const CONTACT_LINKEDIN = "https://www.linkedin.com/in/holovkoivan/";
export const CONTACT_GITHUB = "https://github.com/IvanNavin";
