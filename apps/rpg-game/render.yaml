services:
  - type: web
    name: rpg-game
    env: node
    rootDir: apps/rpg-game
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
